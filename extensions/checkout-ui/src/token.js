/**
 * Placeholder for Shopify getSessionToken() call from the checkout extension runtime.
 * In a real extension, you'd import from '@shopify/checkout-ui-extensions' and call getSessionToken().
 */
export async function getSessionTokenStub() {
  // TODO: replace with real getSessionToken integration inside the extension runtime
  return "dev.stub.jwt";
}

